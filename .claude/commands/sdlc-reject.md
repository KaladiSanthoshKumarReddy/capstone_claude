---
description: Reject the current pipeline stage and re-run it with feedback
---

The current pipeline stage output is REJECTED. Re-run the same stage agent with the user's feedback as additional context. After re-running, print the gate message again and STOP. Do not advance to the next stage — the user must explicitly approve before moving on.
